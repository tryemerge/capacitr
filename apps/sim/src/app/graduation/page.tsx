import GraduationSandbox from "@/components/GraduationSandbox";

export const metadata = {
  title: "Graduation Simulator — Bonding Curve to DEX Lifecycle",
  description: "Simulate the full token lifecycle: bonding curve, graduation, and DEX migration with work token bonding.",
};

export default function GraduationPage() {
  return <GraduationSandbox />;
}
