import Walkthrough from "@/components/Walkthrough";

export const metadata = {
  title: "Capacitor Walkthrough — Step-by-Step Deliberation",
  description: "Walk through a full governance deliberation: entry, conversation, settlement.",
};

export default function WalkthroughPage() {
  return <Walkthrough />;
}
